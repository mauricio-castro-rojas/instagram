<%= form_with(model: [@account, post], local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %><br>
    <%= form.rich_text_area :text %>
  </div>

  <div class="field">
    <%= form.file_field :post_image %>
  </div>

  <p>
    <%= form.submit %>
  </p>
<% end %>