<div>
  <h1 class="font-bold text-4xl">Accounts#home</h1>
  <p>Find me in app/views/accounts/home.html.erb</p>
  <% if account_signed_in? %>
    Signed in as <%= current_account.email %>
    <%= link_to "Logout", destroy_account_session_path, data: { "turbo-method": :delete }%>
  <% end %>

</div>
